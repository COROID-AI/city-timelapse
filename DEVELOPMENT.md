# Development commands

Install dependencies and start the Vite development server:

```sh
npm install
npm run dev
```

Run strict TypeScript checking and produce the production bundle:

```sh
npm run build
```

`PORT` selects the development or preview port; it defaults to `4173`.
