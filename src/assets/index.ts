/**
 * Static and procedural asset directory.
 *
 * Holds generated textures, fonts, and any binary assets. Later tasks may
 * add files here; the folder exists so no task needs to invent it.
 */
export const assetsDir = 'src/assets';
