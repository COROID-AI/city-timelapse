/**
 * Storefronts & advertisements scene subsystem.
 *
 * Public surface: the `Storefronts` component with the
 * instantiate → attach → update → dispose lifecycle, plus the typed state
 * model. Consumes the era registry and city block layout read-only.
 *
 * @packageDocumentation
 */

export * from './types.js';
export * from './content.js';
export * from './storefronts.js';