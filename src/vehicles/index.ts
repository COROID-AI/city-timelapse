/**
 * Vehicles module (placeholder).
 *
 * Owns era-specific stylized vehicles (cars, trolleys, and future drones).
 * Later tasks register vehicle modules via the SceneRegistry.
 */
export const vehiclesModule = 'vehicles';
