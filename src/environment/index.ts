/**
 * Environment module (placeholder).
 *
 * Owns sky, sun, fog, atmosphere, and lighting per era. Later tasks build
 * these visuals and register them through the SceneRegistry.
 */
export const environmentModule = 'environment';
