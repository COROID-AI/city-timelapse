/**
 * Pedestrians module (placeholder).
 *
 * Owns era-specific pedestrian outfits and walking behavior on sidewalks.
 * Later tasks register pedestrian modules via the SceneRegistry.
 */
export const pedestriansModule = 'pedestrians';
