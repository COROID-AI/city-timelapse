import { useEraConfig } from '../hooks/useEraConfig'

interface PostProcessingProps {
  era: number
}

export function PostProcessing({ era }: PostProcessingProps) {
  // Postprocessing is handled through lighting and color adjustments
  // The bloom effect is achieved through emissive materials
  return null
}