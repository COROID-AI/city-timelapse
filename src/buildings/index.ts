/**
 * Buildings module (placeholder).
 *
 * Owns per-era building facades, window emissives, heights, and rooftop
 * props. Later tasks register building modules via the SceneRegistry.
 */
export const buildingsModule = 'buildings';
