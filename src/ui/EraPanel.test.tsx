import { describe, it, expect } from 'vitest';
import { EraPanel } from './EraPanel';

describe('EraPanel', () => {
  it('exports EraPanel component', () => {
    expect(typeof EraPanel).toBe('function');
  });
});
