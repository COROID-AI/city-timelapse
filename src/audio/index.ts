/**
 * Audio module (placeholder).
 *
 * Owns procedural Web Audio SFX and the era-aware crossfade mixer. Later
 * tasks synthesize ambient beds, traffic loops, and event one-shots here —
 * no external audio files.
 */
export const audioModule = 'audio';
