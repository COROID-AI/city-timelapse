/**
 * Street props module (placeholder).
 *
 * Owns lamp posts, hydrants, benches, trees, traffic lights, billboards,
 * and storefronts per era. Later tasks register these via the SceneRegistry.
 */
export const propsModule = 'props';
