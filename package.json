{
  "name": "city-era-timelapse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "City time period timelapse 1945-2025 - era registry, transition state machine and event bus.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:era": "vitest run src/eras/eraSystem.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
