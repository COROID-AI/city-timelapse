{
  "name": "city-era-timelapse",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A 3D city block that morphs across six eras (1945-2055).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
