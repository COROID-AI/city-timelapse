{
  "name": "city-timelapse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "City Time Period Timelapse — a 3D city block that transforms across five decades.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "dev": "npx --yes http-server . -p ${PORT:-5173} -c-1 --cors"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.4.5"
  }
}
