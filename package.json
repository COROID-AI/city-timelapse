{
  "name": "city-time-period-timelapse",
  "version": "0.1.0",
  "description": "Shared city-timelapse era domain: contracts, dataset, and interpolation engine.",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "typescript": "^5.5.4"
  }
}
