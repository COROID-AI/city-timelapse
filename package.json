{
  "name": "city-block-timelapse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "3D city block era timelapse (1945-2055): Vite + TypeScript + three.js scaffold with a headless SceneRuntime and shared block layout contract.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  }
}
