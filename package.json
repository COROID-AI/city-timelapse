{
  "name": "classic-super-mario-browser",
  "version": "0.1.0",
  "description": "A vanilla JS + HTML5 Canvas platformer (no runtime dependencies).",
  "private": true,
  "scripts": {
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/preset-env": "^7.24.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0"
  }
}
